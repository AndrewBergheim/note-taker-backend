{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "Note taking application that stores notes from the user on a server so they can be accessed from anywhere",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Bergheim",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  }
}
